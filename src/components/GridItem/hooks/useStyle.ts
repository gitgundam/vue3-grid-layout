import { Ref, ref } from 'vue'
import { xyToPosition } from '@/core/xyToPosition'

export const useStyle = (gridProps: Ref<any>, props: any) => {
  const style = ref({})
  const updateStyle = () => {}
  return {}
}
